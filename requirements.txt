# ════════════════════════════════════════════════════════════════════
# Розумний Дачник — Backend Requirements
# ВИПРАВЛЕНО: додано slowapi, python-json-logger, PyJWT замість python-jose
# ════════════════════════════════════════════════════════════════════

# ── Web Framework ─────────────────────────────────────────────────
fastapi==0.137.0
uvicorn[standard]==0.49.0
gunicorn==21.2.0          # Production WSGI server
python-multipart==0.0.32  # File uploads
itsdangerous==2.2.0       # Signed cookie sessions for internal admin

# ── Database ──────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0           # Async PostgreSQL driver
psycopg2-binary==2.9.12   # Sync driver (Celery)
alembic==1.18.4           # DB migrations

# ── Auth ──────────────────────────────────────────────────────────
PyJWT[crypto]==2.13.0       # JWT encode/decode; replaces vulnerable python-jose
passlib[bcrypt]==1.7.4
bcrypt==4.0.1             # FIX: явна версія для сумісності з Python 3.12

# ── Rate Limiting (S-04 FIX) ──────────────────────────────────────
slowapi==0.1.10            # Rate limiting для FastAPI

# ── Celery ────────────────────────────────────────────────────────
celery==5.6.3
redis==5.2.0
flower==2.0.1
celery-redbeat==2.3.3

# ── HTTP ──────────────────────────────────────────────────────────
httpx==0.28.1

# ── Config & Validation ───────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0    # Для EmailStr у Pydantic v2

# ── Storage ───────────────────────────────────────────────────────
aioboto3          # Async S3 client
boto3             # Sync S3 (fallback)

# ── Push Notifications ────────────────────────────────────────────
firebase-admin==6.6.0

# ── Payments ─────────────────────────────────────────────────────
stripe==11.3.0

# ── Logging (R-02 FIX) ────────────────────────────────────────────
python-json-logger==2.0.7  # Structured JSON logging

# ── Monitoring (опційно) ──────────────────────────────────────────
sentry-sdk[fastapi,sqlalchemy,celery]==2.62.0
prometheus-fastapi-instrumentator==7.0.0

# ── Utils ─────────────────────────────────────────────────────────
python-dateutil==2.9.0

# ────────────────────────────────────────────────────────────────────
# DEV / TEST dependencies (не включати в production image!)
# Встановлювати окремо: pip install -r requirements-dev.txt
# ────────────────────────────────────────────────────────────────────
# pytest==8.3.0
# pytest-asyncio==0.24.0
# pytest-cov==5.0.0
# httpx==0.28.1
# respx==0.21.1
redis[hiredis]>=5.0
ephem
